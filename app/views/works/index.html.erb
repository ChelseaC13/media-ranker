<h3><span class="green-with-space">List of Works</span></h3>
<h4>Movies</h4>
<table class="table">
    <tr>
        <th>Votes</th>
        <th>Title</th>
        <th>Created By</th>
        <th>Published</th>
        <th>Upvote</th>
    </tr>

    <% work_group = @work.where("category = ?", "movie")%>
        <% work_group.each do |movie| %>
        <tr>
            <td><%= Work.vote_total(movie.id) %></td>
            <td><%= link_to movie.title, work_path(movie.id) %></td>
            <td><%= movie.creator %></td>
            <td><%= movie.publication_year %></td>
            <td class="btn btn-primary "><%="Upvote"%></td>
        </tr>
        <% end %>
</table>


<h4>Books</h4>
<table class="table">
    <tr>
        <th>Votes</th>
        <th>Title</th>
        <th>Created By</th>
        <th>Published</th>
        <th>Upvote</th>
    </tr>

    <% work_group = @work.where("category = ?", "book")%>
        <% work_group.each do |book| %>
        <tr>
            <td><%= "#  votes" %></td>
            <td><%= link_to book.title, work_path(book.id) %></td>
            <td><%= book.creator %></td>
            <td><%= book.publication_year %></td>
            <td class=" btn btn-primary "><%="Upvote"%></td>
        
        </tr>
        <% end %>
</table>


<h4>Albums</h4>
<table class="table">
    <tr>
        <th>Votes</th>
        <th>Title</th>
        <th>Created By</th>
        <th>Published</th>
        <th>Upvote</th>
    </tr>

    <% work_group = @work.where("category = ?", "album")%>
        <% work_group.each do |album| %>
        <tr>
            <td><%= "#  votes" %></td>
            <td><%= link_to album.title, work_path(album.id) %></td>
            <td><%= album.creator %></td>
            <td><%= album.publication_year %></td>
            <td class=" btn btn-primary "><%="Upvote"%></td>
        </tr>
        <% end %>
</table>



